import Foundation

/// - TODO: Understand the bit mask toggling
/// Bitwise
/// https://leetcode.com/articles/single-number-ii/
func singleNumber(_ nums: [Int]) -> Int {
    // var hashSet = Dictionary<Int,Int>()
    // for num in nums {
    //     if hashSet.contains(num) {
    //         //hashSet[hashSet.index(hashSet.startIndex, offsetBy: num)] += 1
    //         //hashSet.update(with: Int)
    //     }
    // }
    return 0
}